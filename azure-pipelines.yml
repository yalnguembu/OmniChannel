trigger:
  branches:
    include:
    - main
    - develop

pool:
  vmImage: 'windows-latest'

variables:

  reactProjectPath: 'FujiPayWeb' 

jobs:
# --- JOB 1 : Build pour la Production ---
- job: Build_Production
  condition: and(succeeded(), eq(variables['Build.SourceBranch'], 'refs/heads/main'))
  displayName: 'Build Production Artifact (main branch)'
  variables:
    apiUrl: 'https://api.fujisatpay.com/api'

  steps:
  - task: NodeTool@0
    displayName: 'Install Node.js'
    inputs:
      versionSpec: '22.x'

  - task: PowerShell@2
    displayName: 'Prepare .env file for Production'
    inputs:
      targetType: 'inline'
      # Le script doit maintenant fonctionner dans le sous-dossier
      script: |
        $projectPath = Join-Path -Path "$(System.DefaultWorkingDirectory)" -ChildPath "$(reactProjectPath)"
        Copy-Item -Path (Join-Path -Path $projectPath -ChildPath ".env.example") -Destination (Join-Path -Path $projectPath -ChildPath ".env")
        $content = Get-Content (Join-Path -Path $projectPath -ChildPath ".env")
        $newContent = $content -replace '__ApiBaseUrl__', '$(apiUrl)'
        Set-Content -Path (Join-Path -Path $projectPath -ChildPath ".env") -Value $newContent
        Write-Host "Le fichier .env a été configuré pour la Production."

  - task: Npm@1
    displayName: 'Install Dependencies (npm install)'
    inputs:
      command: 'install'
      workingDirectory: '$(reactProjectPath)' 

  - task: Npm@1
    displayName: 'Build React App for Production'
    inputs:
      command: 'custom'
      customCommand: 'run build:prod'
      workingDirectory: '$(reactProjectPath)' 

  # Archivage du résultat (on pointe maintenant vers le bon dossier de build)
  - task: ArchiveFiles@2
    displayName: 'Archive Production build'
    inputs:
      # Le chemin racine est maintenant le dossier du projet React
      rootFolderOrFile: '$(System.DefaultWorkingDirectory)/$(reactProjectPath)/build'
      includeRootFolder: false
      archiveType: 'zip'
      archiveFile: '$(Build.ArtifactStagingDirectory)/drop-prod.zip'

  # Publication de l'artefact de production
  - task: PublishBuildArtifacts@1
    displayName: 'Publish Production Artifact'
    inputs:
      PathtoPublish: '$(Build.ArtifactStagingDirectory)/drop-prod.zip'
      ArtifactName: 'drop-prod'


# --- JOB 2 : Build pour le Sandbox ---
- job: Build_Sandbox
  condition: and(succeeded(), eq(variables['Build.SourceBranch'], 'refs/heads/develop'))
  displayName: 'Build Sandbox Artifact (develop branch)'
  variables:
    apiUrl: 'https://test-api.fujisatpay.com/api'

  steps:
  - task: NodeTool@0
    displayName: 'Install Node.js'
    inputs:
      versionSpec: '22.x'

  - task: PowerShell@2
    displayName: 'Prepare .env file for Sandbox'
    inputs:
      targetType: 'inline'
      script: |
        $projectPath = Join-Path -Path "$(System.DefaultWorkingDirectory)" -ChildPath "$(reactProjectPath)"
        Copy-Item -Path (Join-Path -Path $projectPath -ChildPath ".env.example") -Destination (Join-Path -Path $projectPath -ChildPath ".env")
        $content = Get-Content (Join-Path -Path $projectPath -ChildPath ".env")
        $newContent = $content -replace '__ApiBaseUrl__', '$(apiUrl)'
        Set-Content -Path (Join-Path -Path $projectPath -ChildPath ".env") -Value $newContent
        Write-Host "Le fichier .env a été configuré pour le Sandbox."

  - task: Npm@1
    displayName: 'Install Dependencies (npm install)'
    inputs:
      command: 'install'
      workingDirectory: '$(reactProjectPath)' 

  - task: Npm@1
    displayName: 'Build React App for Sandbox'
    inputs:
      command: 'custom'
      customCommand: 'run build'
      workingDirectory: '$(reactProjectPath)' 

  # Archivage du résultat
  - task: ArchiveFiles@2
    displayName: 'Archive Sandbox build'
    inputs:
      rootFolderOrFile: '$(System.DefaultWorkingDirectory)/$(reactProjectPath)/build' 
      includeRootFolder: false
      archiveType: 'zip'
      archiveFile: '$(Build.ArtifactStagingDirectory)/drop-sandbox.zip'

  # Publication de l'artefact de sandbox
  - task: PublishBuildArtifacts@1
    displayName: 'Publish Sandbox Artifact'
    inputs:
      PathtoPublish: '$(Build.ArtifactStagingDirectory)/drop-sandbox.zip'
      ArtifactName: 'drop-sandbox'