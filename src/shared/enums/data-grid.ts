// Sort direction enum
export const enum SortDirection {
  ASC = "asc",
  DESC = "desc",
  NONE = "none",
}

// Table size enum
export const enum TableSize {
  SMALL = "sm",
  MEDIUM = "md",
  LARGE = "lg",
}
